import uvm_pkg::*;

class my_transaction extends uvm_sequence_item;
  /*
  rand real real_A, real_B, real_Z;
  logic ENABLE, READY;
  rand logic [63:0] A, B, Z;
  
  `uvm_object_utils(my_transaction)
  function new(string name = "my_transaction");
    super.new(name);
  endfunction */
endclass
